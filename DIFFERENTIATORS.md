# StreamDeck Differentiators

## Positioning
StreamDeck should win by feeling like a real premium streaming product, not a utility panel wrapped around IPTV credentials. The wedge is simple: faster switching, better browse context, household-friendly controls, and power-user playback insight without making the whole app feel technical.

For the first credible proof, three surfaces matter more than everything else: Login has to feel trustworthy instead of scary, Home has to prove this is a product and not a control panel, and Live has to surf fast enough that users stop thinking about the provider. The mock Xtream adapter should reinforce those surfaces directly, not sit beside them as a separate dev-only toy.

| Feature | One-line pitch | Competitive gap | Build phase | Architecture notes |
| --- | --- | --- | --- | --- |
| Multi-connection switching | Save multiple providers and jump between them instantly without re-entering credentials. | TiviMate and IPTV Smarters can store multiple sources, but neither makes hot-swap feel like a first-class browse flow. Flix IPTV and iMPlayer still lean on setup-style connection management instead of fast provider switching. | Phase 1 foundation, Phase 2 polish | Model providers as first-class connection records in local storage and Zustand. Every favorite, watch-history item, EPG cache, search entry, and continue-watching record must be keyed by `connectionId` from day one. |
| Smart EPG overlay | Show NOW and NEXT inline on every channel card so users never leave the grid to know what is airing. | TiviMate, IPTV Smarters, Flix IPTV, and iMPlayer all rely more heavily on separate guide views, side panels, or deeper navigation. None makes inline NOW and NEXT the default browse pattern. | Phase 1 | Normalize short EPG into a small `nowNext` payload, cache by provider + stream, and render directly on cards with stale-while-revalidate refresh. |
| Continue Watching across live + VOD | One resume rail for everything the user was watching, whether live TV, replay, or VOD. | TiviMate is stronger on live continuity than the others, but none of TiviMate, IPTV Smarters, Flix IPTV, or iMPlayer delivers a truly unified live plus VOD resume model, especially once one provider degrades and the saved state needs a fallback path. | Phase 1 foundation, Phase 2 deepen | Use one canonical playback-session model with provider ID, content kind, position, recency, playback URL, artwork, and episode metadata. Live sessions store recency and channel context, VOD stores position. Dedicated Favorites, Continue Watching, and in-library resume rails should consume the same provider-aware content graph and point to alternate provider copies when the original source expires or saturates. |
| Instant channel preview | Hover or focus on a channel card to get a live preview without leaving the browsing grid. | This is basically missing as a polished default in TiviMate, IPTV Smarters, Flix IPTV, and iMPlayer. Most competitors force a full navigation jump into playback. | Phase 1 prototype, Phase 2 optimize | Keep one active muted preview player, aggressively reuse the element, rate-limit preview swaps, and fall back to channel art when autoplay or bandwidth rules fail. |
| Folder / playlist organization | Let users build custom folders like Game Day, Morning News, or Kids Bedtime beyond favorites. | TiviMate and iMPlayer expose stronger customization than Flix IPTV or IPTV Smarters, but none gives users a clean, mainstream custom-folder system that feels like streaming-library curation, especially once the folder can recover through healthier provider copies when one source expires or saturates. | Phase 1.5 live, Phase 2 deepen | Create local folder entities plus ordered membership tables keyed by provider-aware content IDs. Saved smart filters can later become auto-updating folders. Phase 1.5 now includes real local collections in the prototype shell plus alternate-provider launch paths from inside a collection. |
| One-click recording | Start recording live TV to local storage or a user-chosen destination with one action. | TiviMate has the clearest reputation here, but a polished web-first recording path is still rare across TiviMate, IPTV Smarters, Flix IPTV, and iMPlayer. | Phase 2+ | Abstract recording behind a capture service so browser, desktop, and native targets can diverge later without touching playback UI. |
| Search across all providers | Search once and get ranked results across every saved IPTV connection. | TiviMate, IPTV Smarters, Flix IPTV, and iMPlayer usually search only inside the active playlist or provider, not across the full saved library. | Phase 1 prototype live, Phase 2 polish | Build a merged client-side index with provider labels, fuzzy matching, duplicate suppression, and ranking tuned for live relevance plus recency. The prototype can ship with on-demand client fetches first, then harden into a persistent index. |
| Cinematic detail-first browsing | Let Movies and Series feel like a premium streaming catalog with instant context, category chips, and resume-aware detail rails instead of a dead poster wall. | IPTV Smarters Pro and Flix IPTV especially fall back to utility-grid browsing, while TiviMate and iMPlayer prioritize TV power features over premium on-demand discovery. | Phase 1.5 live, Phase 2 deepen | Reuse the provider-aware catalog cache and playback history inside Movies and Series so the first paint is instant, category filters stay local, and the selected detail panel can drive play, resume, and episode drill-down without refetching the whole library. |
| Watch party / sync viewing | Share a link and keep multiple viewers synced to the same stream in the same moment. | None of TiviMate, IPTV Smarters, Flix IPTV, or iMPlayer has made synchronized social viewing a real product feature. | Phase 3 | Needs a lightweight session backend for room state, stream metadata, clock sync, and drift correction. Keep playback sessions durable now so sync can layer on later. |
| Parental controls with per-profile PINs | Each household profile gets its own maturity gates and PIN instead of one blunt global lock. | TiviMate, IPTV Smarters, Flix IPTV, and iMPlayer lean toward coarse global locks, not profile-aware household controls. | Phase 2 | Add profile records, provider-aware content labels, PIN verification flows, and route guards for live, movies, and series. |
| Stream health indicator | Surface bitrate, buffer health, codec, and playback quality in a subtle power-user HUD. | TiviMate and iMPlayer have more enthusiast credibility than the others, but across all four apps playback diagnostics are still hidden, thin, or debug-like instead of being a polished user-facing HUD. | Phase 1.5 to Phase 2 | Feed HLS.js telemetry into a shared player store, reduce it to a simple health model, and surface it in a compact overlay that casual users can ignore. |
| Provider trust cockpit | Show account expiry, connection capacity, timezone, readiness cues, and the right recovery action directly on Login, Home, Live, Search, and Settings so users trust the source before they trust the stream. | Competitors usually bury provider/account truth in setup menus or leave users guessing why playback fails when a line is maxed or expired, and they rarely suggest the next best move in context. | Phase 1 prototype live, Phase 2 deepen | Persist a lightweight auth summary per saved connection, downgrade provider status when lines are maxed or credentials expire, and let mock health expose demo-ready account posture plus recommended recovery actions so failure rehearsal covers trust signals and operator response together. |
| Rehearsable provider resilience | Let the team intentionally simulate degraded live, degraded search, and degraded guide conditions inside the product before a real provider fails on demo day. | Competitors mostly treat provider instability as support debt, not a product-quality discipline. TiviMate, IPTV Smarters, Flix IPTV, and iMPlayer do not meaningfully turn failure rehearsal into a first-class build advantage. | Phase 1 prototype live, Phase 2 deepen | Keep a mock Xtream adapter with scenario-based endpoint degradation, including line saturation, expired-account, and auth-instability rehearsal, and wire Login, Home, Live, Search, Movies, and Series to show explicit fallback UX, operator headlines, one-click rehearsal toggles, and verification steps instead of generic errors. |
| Mock provider manifest | Make the fake Xtream source describe exactly what Login, Home, and Live are proving so the product story, demo path, and adapter contract stay aligned. | Competitors do not treat their provider layer like a demoable product primitive at all, which is why setup, browsing, and failure rehearsal drift apart. | Phase 1 prototype live | Add a manifest endpoint that advertises sample credentials, supported screens, differentiators, and a demo checklist, then render that contract inside Login, Home, and Live so the original scaffold brief is visible in-product. |
| Shared proof launch matrix | Give Login, Home, and Live one shared launch contract so the next demo step and the recovery step stay explicit on every proof surface. | Competitors do not explain the intended browse-to-launch path inside the product, so setup, discovery, and fallback actions drift into support knowledge instead of product truth. | Phase 1 prototype live | Extend the mock provider manifest with per-surface primary and recovery launch actions plus operator prompts, then render the same contract through a shared proof panel on Login, Home, and Live. |
| Operator proof command center | Give Login, Home, and Live the same proof journey, verification target, success signal, and scenario spotlight so the demo path and the failure rehearsal path never split apart. | Competitors do not publish an in-product launch checklist for setup, browse, and degraded-state recovery, which is why demos often depend on tribal knowledge and support-style narration. | Phase 1 prototype live | Extend the mock provider manifest with a proof journey, per-surface verification targets, launch verification steps, and scenario spotlights, then render them through one shared proof shell on Login, Home, and Live. |
| Shared launch ops console | Put one adapter-driven rehearsal console on Login, Home, and Live so scenario switching, endpoint health, operator goals, and the active recovery path stay synchronized on every proof surface. | Competitors may expose status pages or ad hoc warnings, but they do not run setup, browse, and failure rehearsal through the same in-product operations shell, which is why support knowledge and demo knowledge drift apart. | Phase 1 prototype live | Extend the mock provider manifest with command-center copy and per-surface playbooks, then render a shared operations console on Login, Home, and Live instead of hand-maintaining separate rehearsal blocks by screen. |
| Shared proof scorecards | Give Login, Home, and Live one measurable launch-confidence board so setup safety, browse readiness, preview readiness, and recovery confidence read like product truth instead of demo narration. | Competitors do not quantify whether a surface is launch-ready, fallback-ready, or recovery-led in-product, which is why trust and readiness stay buried in support talk rather than visible product signals. | Phase 1 prototype live | Extend the mock provider manifest with per-surface proof scorecards and render them through the shared operations console so every core surface shows the same adapter-driven readiness metrics. |
| Shared exit criteria board | Give Login, Home, and Live one adapter-driven go/hold board so the team knows exactly when to advance, when to pause, and what recovery move owns the next step on each proof surface. | Competitors do not publish in-product exit criteria for setup, browse, and recovery, so demos rely on tribal knowledge and support-style improvisation instead of visible product truth. | Phase 1 prototype live | Extend the mock provider manifest with per-surface exit criteria, hold signals, next-hop targets, and recovery ownership, then render the same board on Login, Home, and Live through the shared operations console. |
| Shared handoff map | Give Login, Home, and Live one adapter-driven handoff contract so each proof surface shows what context carries forward, why the next move is safe, and which fallback preserves the user’s intent if the handoff degrades. | Competitors rarely explain what setup, browse, and playback context survives the jump between surfaces, which is why provider failure often feels like a restart instead of a guided recovery. | Phase 1 prototype live | Extend the mock provider manifest with per-surface handoff maps, preserved context, confidence cues, and fallback handoff language, then render the same board inside the shared operations console on Login, Home, and Live. |
| Shared escalation ladder | Give Login, Home, and Live one adapter-driven recovery ladder so each proof surface shows the first move, the second move, and the last safe fallback before the user gets stranded in guesswork. | Competitors may show warnings, but they rarely publish a consistent escalation order in-product, so users and operators bounce between retries, settings, and support instincts instead of following one obvious rescue path. | Phase 1 prototype live | Extend the mock provider manifest with per-surface escalation ladders, trigger states, and final fallback ownership, then render the same ladder inside the shared operations console on Login, Home, and Live. |
| Scenario impact matrix | Give Login, Home, and Live one adapter-driven matrix that shows how healthy launch, live degradation, guide degradation, line saturation, expired accounts, and auth instability change each surface before the user walks into a dead end. | Competitors may surface one-off warnings, but they rarely map failure modes across setup, browse, and surf flow in-product, which is why users blame the wrong layer and recovery becomes support guesswork. | Phase 1 prototype live | Extend the mock provider manifest with per-surface scenario impact cards and render the same matrix inside the shared operations console on Login, Home, and Live so every rehearsal mode publishes its user impact and the next recommended move. |
| Surface promise stack | Give Login, Home, and Live one adapter-driven promise board that states what the screen proves right now, what it protects when the provider weakens, and why the next move is still safe. | Competitors may show status and warnings, but they rarely translate that into plain-English promises about what the user can trust on the current surface, which is why degraded states feel like guesswork instead of controlled recovery. | Phase 1 prototype live | Extend the mock provider manifest with per-surface promise stacks and render them in the shared operations console on Login, Home, and Live so every proof surface publishes its current proof, protection guarantee, and next-safe-move language directly from the adapter. |
| Surface evidence ledger | Give Login, Home, and Live one adapter-driven provenance board that shows what the screen knows from live provider truth, what is protected by cache, and what the shell is safely inferring during rehearsal. | Competitors almost never distinguish between live truth, cached truth, and inferred fallback state, so users are left guessing whether a warning means “broken,” “stale,” or “still safe enough to continue.” | Phase 1 prototype live | Extend the mock provider manifest with per-surface evidence ledgers and render them through the shared operations console on Login, Home, and Live so every proof surface publishes its live evidence, cached protection, and inference boundary directly from the adapter. |
| Surface freshness budget | Give Login, Home, and Live one adapter-driven freshness board that states how long each critical claim can stay trustworthy, what fallback remains safe when refresh misses, and when the surface must switch from live proof to recovery. | Competitors rarely tell users whether a browse or trust cue is still within a safe freshness window, which is why stale-but-usable product state gets mistaken for either live certainty or total failure. | Phase 1 prototype live | Extend the mock provider manifest with per-surface freshness budgets and render them directly on Login, Home, and Live plus the shared operations console so every proof surface publishes its refresh window, safe fallback window, and forced-recovery trigger from the adapter. |
| Surface contradiction board | Give Login, Home, and Live one adapter-driven contradiction board that states which signal wins when live truth, cache continuity, and recovery guidance disagree, so the screen never tells two conflicting stories at once. | Competitors often show stale guide cues, healthy-looking launch buttons, and buried trust warnings at the same time, forcing users to guess which signal matters most. | Phase 1 prototype live | Extend the mock provider manifest with per-surface contradiction boards and render them on Login, Home, and Live plus the shared operations console so every proof surface publishes the conflicting signals, the winning truth, and the suppression rule directly from the adapter. |
| Surface reset boundary | Give Login, Home, and Live one adapter-driven reset board that states what can refresh in place, what user context is preserved, and what event actually forces a hard reset so recovery never feels random. | Competitors often bounce users back to the start when a provider weakens, even when most of the screen context could have survived in place. | Phase 1 prototype live | Extend the mock provider manifest with per-surface reset boundaries and render them on Login, Home, and Live plus the shared operations console so every proof surface publishes its in-place refresh zone, preserved context, and hard-reset trigger directly from the adapter. |
| Surface action gate | Give Login, Home, and Live one adapter-driven action gate board that states which action is fully live, which action must downgrade first under provider stress, and what unlocks the premium path again. | Competitors often keep the brightest CTA visually live even when the safest next move has already changed, so users click into predictable failure instead of guided recovery. | Phase 1 prototype live | Extend the mock provider manifest with per-surface action gates and render them on Login, Home, and Live plus the shared operations console so every proof surface publishes its primary action, downgraded fallback action, and unlock condition directly from the adapter. |
| Surface intent lock | Give Login, Home, and Live one adapter-driven intent-lock board that states which user intent stays protected, what can drift around it, and what actually breaks that promise under provider stress. | Competitors often preserve visuals or buttons but still silently break the user’s original intent, forcing them to reconstruct setup, browse, or surf context after the provider weakens. | Phase 1 prototype live | Extend the mock provider manifest with per-surface intent locks and render them on Login, Home, and Live plus the shared operations console so every proof surface publishes its protected intent, allowed drift, and true break condition directly from the adapter. |
| Surface explanation boundary | Give Login, Home, and Live one adapter-driven explanation board that states what the UI must say explicitly, what can stay implied, and what event forces blunt recovery language so the product never hides critical provider truth behind premium gloss. | Competitors often swing between under-explaining degraded states and dumping support-style copy everywhere, which makes trust warnings either too vague or too noisy. | Phase 1 prototype live | Extend the mock provider manifest with per-surface explanation boundaries and render them on Login, Home, and Live plus the shared operations console so every proof surface publishes its required disclosure, ambient confidence zone, and forced-disclosure trigger directly from the adapter. |

## Competitive gap snapshot

| Competitor | What they do well | Where StreamDeck should beat them first |
| --- | --- | --- |
| TiviMate | TV-first navigation, guide depth, recording reputation | Multi-provider switching, premium web UX, inline browse context, household profile features |
| IPTV Smarters Pro | Broad compatibility, user familiarity, cross-platform availability | Cleaner information architecture, stronger resume, less clutter, better cross-provider search |
| Flix IPTV | Simplicity, easy first run | Richer discovery, custom folders, preview-based surfing, better playback insight |
| iMPlayer | Enthusiast customization, deep controls | Same power under the hood with a much cleaner default experience and stronger family usability |

## Build order this doc implies
1. **Phase 1 now:** saved-provider login, inline NOW/NEXT, home hero + rails, live browser with preview, playback health HUD, rehearsable degraded-provider modes
2. **Phase 2 next:** unified search, continue watching across live + VOD, cinematic on-demand detail rails, custom folders, parental controls, provider management polish, deeper fallback copy for partial outage states
3. **Phase 3 later:** recording adapters and watch-party sync backend

## Architecture calls for Axe
1. Connection management cannot be bolted on later. Provider-aware IDs are mandatory now.
2. The channel card is the primary interaction primitive because EPG, preview, save, folders, and health all hang off it.
3. Playback telemetry belongs in shared state early, even if only a small HUD consumes it first.
4. Search, continue watching, and folders should all sit on one normalized content model across live, VOD, and series.
5. Anything that could become multi-device later, like profiles, recording jobs, watch parties, should use durable IDs and explicit timestamps now.

## Immediate implementation call
- Ship now: saved-provider login, inline NOW/NEXT guide context, live browser preview with graceful artwork fallback, provider-aware history/favorites, stream health HUD, provider trust cockpit account signals on Login/Home/Live/Search/Settings, a global player dock with real resume tracking for movies and series, explicit degraded-provider messaging inside Live and Search, and instant-feeling cached Movies/Series browse surfaces with detail rails
- Ship now too: a formal mock Xtream adapter manifest that declares the sample credentials, primary proof surfaces, screen-level readiness, and demo checklist for Login, Home, and Live so the app itself explains what Phase 1 already proves
- Ship now too: one shared proof-launch matrix for Login, Home, and Live so each of those surfaces exposes the same product contract for the next launch move, the fallback launch move, and the operator prompt directly from the adapter manifest instead of drifting into hand-written UI copy
- Ship now too: one shared operator proof command center for Login, Home, and Live so the same in-product shell carries the proof journey, verification target, success signal, per-surface launch checks, and scenario spotlight without requiring a separate demo script
- Ship now too: one shared launch ops console on Login, Home, and Live so scenario toggles, endpoint health, operator goals, and active recovery instructions all come from the adapter manifest instead of being repeated and drifting by surface
- Ship now too: one shared proof scorecard system on Login, Home, and Live so setup safety, featured-browse readiness, preview confidence, and recovery posture all come from the adapter manifest as measurable surface signals instead of hand-written reassurance copy
- Ship now too: one shared exit-criteria board on Login, Home, and Live so advance signals, hold signals, next hops, and recovery ownership come from the adapter manifest as visible product truth instead of demo tribal knowledge
- Ship now too: one shared handoff map on Login, Home, and Live so preserved context, handoff confidence, safe-next-step language, and fallback handoff moves all come from the adapter manifest instead of drifting into support narration
- Ship now too: one shared escalation ladder on Login, Home, and Live so the first recovery move, second recovery move, trigger state, and final fallback owner all come from the adapter manifest instead of drifting into ad hoc operator judgment
- Ship now too: one shared scenario impact matrix on Login, Home, and Live so healthy launch, live degradation, guide degradation, line saturation, expired-account, and auth-instability rehearsals all explain their surface impact and recommended move directly from the adapter manifest instead of living only in health URLs or team memory
- Ship now too: one shared surface promise stack on Login, Home, and Live so each proof surface states what it proves right now, what it still protects under provider stress, and why the next move is safe directly from the adapter manifest instead of relying on demo narration
- Ship now too: one shared surface evidence ledger on Login, Home, and Live so each proof surface states which claims come from live provider truth, which claims are protected by cache, and where the shell is intentionally inferring safe next moves during rehearsal instead of overclaiming certainty
- Ship now too: one shared surface freshness budget on Login, Home, and Live so each proof surface states how fresh its key trust and browse claims must be, what safe fallback window remains when refreshes miss, and when recovery needs to take over directly from the adapter manifest instead of leaving staleness interpretation to operator guesswork
- Ship now too: one shared surface contradiction board on Login, Home, and Live so each proof surface states which signal wins when live truth, cache continuity, and recovery posture disagree, what message should be suppressed, and which user-facing truth has to stay visible directly from the adapter manifest instead of leaving mixed-state interpretation to operator guesswork
- Ship now too: one shared surface reset boundary on Login, Home, and Live so each proof surface states what can revalidate in place, what user context must survive that revalidation, and which failure actually forces a hard reset directly from the adapter manifest instead of making recovery feel random or punitive
- Ship now too: one shared surface action gate on Login, Home, and Live so each proof surface states which CTA remains premium, which fallback action must take over first when provider trust weakens, and what unlocks the premium path again directly from the adapter manifest instead of leaving the strongest button visually active past the point of safety
- Ship now too: one shared surface intent lock on Login, Home, and Live so each proof surface states which user intent remains protected, what copy or CTA hierarchy may drift around that intent, and what actual event breaks the promise directly from the adapter manifest instead of forcing the user to reconstruct their flow after degraded-provider recovery
- Ship now too: one shared surface explanation boundary on Login, Home, and Live so each proof surface states what provider truth must be explicit, what premium confidence can stay ambient, and what trigger forces blunt recovery language directly from the adapter manifest instead of hiding critical disclosure behind glossy browse or setup copy
- Ship now as demo insurance too: provider-readiness surfaces on Login, Home, Live, Search, Movies, and Series so the mock Xtream adapter advertises sample credentials, category density, preview-friendly capability flags, endpoint-health summaries, line-capacity and expired-account warnings, trust-signal chips, scenario-specific recovery actions, verification steps, one-click degraded-mode rehearsal toggles, hot in-place scenario refresh on the affected surface, automatic mock-provider auth revalidation when rehearsal modes flip, and direct health links in-product instead of hiding them in terminal-only health checks
- Ship next: stronger cross-provider search ranking and persistence, duplicate-aware Search/Favorites/Continue Watching plus Home spotlight rails, Live channel cards, Movies/Series detail surfaces, and the global player dock all pointing to alternate provider copies of the same title directly from the result card, discovery rail, detail surface, or active playback shell instead of hiding recovery behind one winner, intent-preserving provider recovery plans across Login/Home/Live/Search/Settings/Movies/Series/Favorites/Continue Watching/Collections so the healthiest saved provider can take over without dumping the user out of the surface they meant to use, one shared premium recovery rail UI across Search/Settings/player plus Home/Live/Favorites/Continue/Collections and now Movies/Series detail rails so degraded-provider messaging, CTA hierarchy, fallback trust language, and series deep-link actions stop drifting by surface, one shared provider-trust badge primitive across search variants, featured fallbacks, live rescue cards, settings trust surfaces, login trust states, mock rehearsal headlines, saved-provider fact grids, and on-demand detail surfaces so trust tone and recovery hierarchy stop drifting visually too, one shared provider-fact grid system across Login, Home, Live, Search, Settings, Movies, Series, Favorites, Continue Watching, and the player dock so status, expiry, capacity, timezone, and trust posture stop rendering through bespoke stat cards anywhere recovery matters, one canonical provider-variant helper layer under search results, player-dock recovery, detail rails, saved rails, and collections so trust ranking, exact alternate copies, series recovery keys, and same-category live rescue all resolve from one deterministic engine instead of several near-matching local helpers, category-preserving Live fallback so a Sports or News surf session can jump onto the healthiest saved provider even when an exact duplicate channel is missing, the same category-preserving rescue language carried into Home featured/spotlight live rails, collection-launched live items, favorite live cards, continue-watching live resume edges, and player-dock live rescue actions, an auth-unstable rehearsal path where cached context survives failed trust refreshes on Login/Home/Live/Search/Settings/Movies/Series and can launch the healthiest saved provider directly with one tap while preserving season and episode resume context, favorite-series recovery that can auto-start the matched resume episode on either the active or alternate provider when context exists, richer folder smart-rules beyond the now-live collection recovery paths, deeper continue-watching resume accuracy, connection rename/remove/retry actions, partial-result handling when one provider drops during multi-provider search, and a tighter in-product mock rehearsal cockpit that spells out affected endpoints plus the exact degraded UX the team should verify across every browse surface, including canonical player-dock recovery behavior plus shared provider-fact posture during line-saturated, expired-account, and auth-unstable runs
- Hold for later: browser recording constraints, shared watch-party infrastructure
